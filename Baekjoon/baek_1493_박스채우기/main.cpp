#include <iostream>
#include <vector>

using namespace std;
int main() {
    int l, w, h, N;
    cin>>l>>w>>h;
    cin>>N;
    vector<int> boxes();
    for(int i=0; i<N; ++i){
        int m, n;
        cin>>m>>n;
        boxes[n]=
    }
    std::cout << "Hello, World!" << std::endl;
    return 0;
}