#include <iostream>

using namespace std;

int main() {
    int T;
    cin >> T;
    while(T--) {
        int N, M;
        cin >> N >> M;
        for(int i=0; i<M; ++i) {
            int a, b;
            cin >> a >> b;
        }
        cout << N - 1 << '\n';
    }
    //std::cout << "Hello, World!" << std::endl;
    return 0;
}
