#include <iostream>
#include <vector>

using namespace std;

int main() {
    int n;
    cin >> n;
    vector<pair<int, int>> lines(n);
    for (auto &line:lines) {
        cin >> line.first >> line.second;
    }

    for

    std::cout << "Hello, World!" << std::endl;
    return 0;
}