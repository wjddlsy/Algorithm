#include <iostream>
#include <vector>
#include <queue>

using namespace std;

int V,E;

vector<vector<int>> adj;
vector<int> A,B;
int main() {
    int C;
    cin>>C;
    while(C--){
        int M;
        cin>>M;
        for(int i=0; i<M; ++i){
            int a, b;
            cin>>a>>b;
            A.push_back(a);
            B.push_back(b);


        }
    }
    std::cout << "Hello, World!" << std::endl;
    return 0;
}