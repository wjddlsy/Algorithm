#include <iostream>
#include <vector>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int C;
    cin>>C;
    while(C--){
        int N, K;
        cin>>N>>K;
        for(int i=0; i<N; ++i){
            int l, m, g;
            cin>>l>>m>>g;
        }
    }
    std::cout << "Hello, World!" << std::endl;
    return 0;
}