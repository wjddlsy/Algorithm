#include <iostream>
#include <vector>
#include <map>

using namespace std;

int main() {
    int T;
    cin>>T;
    for(int t=1; t<=T; ++t) {
        cout<<"Case #"<<t<<": ";
        int N, G, M;
        cin>>N>>G>>M;
        vector<int> consulate;
        vector<int> clock, anti;
        map<int, int> reta, retc;

        for(int i=0; i<G; ++i) {

        }
        cout<<'\n';
    }
    //std::cout << "Hello, World!" << std::endl;
    return 0;
}