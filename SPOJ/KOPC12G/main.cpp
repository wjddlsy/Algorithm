#include <iostream>
#include <vector>

using namespace std;

int main() {
    int T;
    cin >> T;
    while (T--) {
        int N, q;
        cin >> N >> q;
        while (q--) {
            int oper;
            cin >> oper;
            if (oper == 0) {
                int x, y;
                string str;
                cin >> x >> y >> str;

            } else {
                int x1, y1, x2, y2;
                cin >> x1 >> y1 >> x2 >> y2;

            }
        }
    }
    std::cout << "Hello, World!" << std::endl;
    return 0;
}