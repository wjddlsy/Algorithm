#include <iostream>
#include <vector>

using namespace std;

int main() {
    int n, h, b, e;
    cin>>n>>h>>b>>e;

    vector<int> c(n);
    for(auto &in:c) {
        cin>>in;
    }
    std::cout << "Hello, World!" << std::endl;
    return 0;
}